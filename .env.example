# Discord AI Bot Environment Variables
# Copy this file to .env and fill in your actual values

# Required: Your Discord bot token from https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_discord_bot_token_here

# Required: Secret key for Flask sessions (generate a random string)
FLASK_SECRET_KEY=your_secret_key_here

# Optional: Your Discord server ID (bot works in all servers if not specified)
GUILD_ID=your_guild_id_here

# Optional: Database URL (Railway provides this automatically)
# DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Password for web interface access
# UI_PASSWORD=your_web_interface_password

# Optional: Port for local development (Railway sets this automatically)
# PORT=5000

# Note: Never commit your actual .env file to version control!
# The .gitignore file already excludes .env files for security.
